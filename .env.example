# Conexão com o banco PostgreSQL (camadas silver + gold restauradas).
# Copie este arquivo para `.env` e ajuste para o SEU banco local.
#   cp .env.example .env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bigdata_for_finance
DB_USER=bigdata
DB_PASS=bigdata
DB_PASSWORD=bigdata
